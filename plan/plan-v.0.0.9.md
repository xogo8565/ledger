# 가계부 앱 개발 진행 기록 v.0.0.9

작성일: 2026-06-23

## 1. 기준

- 이전 기록: `plan/plan-v.0.0.8.md`
- 이번 개발 범위: v.0.0.9 권장 항목 중 전월 예산 복사
- 검증 방식: Docker Compose 컨테이너 빌드

## 2. 이번 버전 개발 완료

### 전월 예산 복사 API

- `POST /api/budgets/settings/copy-previous?month=YYYY-MM` API를 추가했다.
- 요청 월의 전월 예산을 찾아 현재 월 예산으로 복사한다.
- 복사 대상:
  - 월 전체 예산
  - 카테고리별 예산
- 현재 월 예산이 없으면 새로 생성한다.
- 현재 월에 이미 카테고리 예산이 있으면 전월 값으로 덮어쓴다.
- 전월 예산이 없으면 오류를 반환한다.

### 예산 설정 UI

- 예산 설정 화면에 `전월 예산 복사` 버튼을 추가했다.
- 버튼 클릭 시 전월 예산 복사 API를 호출한다.
- 복사에 성공하면 현재 예산 설정 폼을 복사된 값으로 갱신한다.
- 전월 예산이 없으면 안내 메시지를 표시한다.

### README 정리

- 구현된 MVP 목록에 전월 예산 복사를 반영했다.
- 다음 작업 목록에서 전월 예산 복사 항목을 제거했다.

## 3. v.0.0.8 대비 상태 변경

| 항목 | v.0.0.8 상태 | v.0.0.9 상태 | 메모 |
| --- | --- | --- | --- |
| 전월 예산 복사 API | 미완료 | 완료 | 전체/카테고리 예산 복사 |
| 예산 설정 UI 복사 버튼 | 미완료 | 완료 | 복사 후 폼 값 갱신 |
| 전월 예산 없음 처리 | 미완료 | 완료 | API 오류와 안내 메시지 |
| Docker 빌드 검증 | 완료 | 완료 | frontend/backend 동시 빌드 통과 |

## 4. 현재 완료로 볼 수 있는 MVP 항목

- 거래 입력: 수입/지출/이체
- 거래 상세 조회
- 거래 수정
- 거래 삭제
- 일일 거래 목록
- 월 요약
- 달력 요약
- 메모 거래 목록
- 자산 목록/생성/수정/삭제
- 카드 자산 생성/수정 시 결제 계좌/확정일/결제일/자동 결제 설정
- 카테고리 목록/생성/수정/삭제
- 월별 전체 예산 및 카테고리 예산 설정
- 카테고리별 예산 사용률과 초과 표시
- 전월 예산 복사
- 월별 수입/지출 통계
- 카테고리별 지출 비중
- 카드 결제 예약 생성/조회/실행/취소
- 카드 결제 예정 금액의 청구 기간 기반 산정
- 카드 결제 중복 실행 방지와 실패 사유 표시
- 할부 거래 생성, 일정 조회, 그룹 단위 수정/삭제
- 반복 거래 생성/수정/삭제/만기 생성
- 영수증 첨부 API 및 신규 거래 첨부 흐름
- 문자 텍스트 파싱 미리보기 초안
- Docker 기반 실행 환경

## 5. 아직 부분 완료인 항목

### 카드 관리

- 카드 결제 예약과 실행은 구현되어 있다.
- 카드 자산 생성/수정 시 카드 프로필 입력과 저장 API 연결은 구현되어 있다.
- 자동 결제 중복 실행 방지는 `PROCESSING` 상태와 낙관적 잠금으로 보강했다.
- 실패 상태 저장과 UI 표시는 구현되어 있다.
- 결제 예정 금액은 다음 결제 예정일에 대응하는 청구 기간 기준으로 산정한다.
- 말일 처리는 구현되어 있다.
- 공휴일/주말 결제일 보정 정책은 아직 미완료다.
- 실패한 카드 결제를 사용자가 다시 예약하거나 재시도하는 UI는 아직 미완료다.

### 할부 관리

- 할부 생성, 조회, 그룹 단위 수정/삭제는 구현되어 있다.
- 그룹 수정 시 기존 회차 수 변경은 아직 미지원이다.
- 카드 결제 예정 금액과 할부 청구 월 연결 검증이 필요하다.

### 예산

- 월별 전체/카테고리 예산 저장은 구현되어 있다.
- 카테고리별 예산 사용률과 초과 표시는 구현되어 있다.
- 전월 예산 복사는 구현되어 있다.

### 통계

- 월별 및 카테고리별 지출 통계는 구현되어 있다.
- 연별 통계, 기간 필터, 카테고리 선택 후 거래 목록 이동은 미완료다.

### 영수증 첨부

- 신규 거래 첨부와 업로드 API는 구현되어 있다.
- 첨부 이미지 조회, 미리보기, 삭제, 다중 첨부 UI는 미완료다.

### 문자 자동 입력

- 원문 붙여넣기와 후보 추출은 구현되어 있다.
- 파싱 결과를 거래 입력 폼으로 확정 저장하는 흐름은 미완료다.
- 카드사/은행별 파싱 규칙과 카테고리 추천은 고도화가 필요하다.

### 공동 가계부

- Household/Member 도메인은 있다.
- 인증, 초대, 권한, 사용자 전환 UI는 미완료다.
- 인증, 권한, 사용자 전환은 불필요하더라도 자산 명의나 관련 소비 태그로 활용할 구조가 필요하다.

## 6. 다음 개발 우선순위

### v.0.0.10 권장

1. 실패한 카드 결제 재시도/재예약 UI
2. 카드 결제일 휴일 보정 정책
3. 할부 회차 수 변경 지원
4. 검색/필터
5. 영수증 이미지 조회/삭제 UI

### v.0.0.11 권장

1. 문자 파싱 결과를 거래 입력 폼으로 확정 저장
2. 연별 통계와 카테고리 선택 상세 이동
3. 데이터 내보내기
4. 자산 명의/소비 태그 구조
5. 프론트엔드 화면 단위 컴포넌트 분리

## 7. 검증

- `docker compose build frontend backend` 통과

## 8. 개발 메모

- 전월 예산 복사는 현재 월 예산을 전월 값으로 덮어쓴다.
- 전월에는 있었지만 현재 비활성화된 카테고리 예산도 저장 데이터 기준으로 복사된다. 화면에는 활성 지출 카테고리만 표시된다.
- 로컬 PATH에서 `npm`, `git`을 찾지 못했고 백엔드에는 Gradle wrapper가 없어 로컬 직접 빌드는 실행하지 못했다.
- 프로젝트 기준 검증 방식인 Docker Compose 빌드는 정상 통과했다.
