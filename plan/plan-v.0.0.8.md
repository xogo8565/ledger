# 가계부 앱 개발 진행 기록 v.0.0.8

작성일: 2026-06-23

## 1. 기준

- 이전 기록: `plan/plan-v.0.0.7.md`
- 이번 개발 범위: v.0.0.8 권장 항목 중 카테고리별 예산 사용률과 초과 표시
- 검증 방식: Docker Compose 컨테이너 빌드

## 2. 이번 버전 개발 완료

### 카테고리별 예산 사용률 API

- 월 요약 응답(`MonthlySummaryDto`)에 `categoryBudgetUsages`를 추가했다.
- 카테고리별 예산 사용 정보에 다음 값을 포함한다.
  - 카테고리 ID
  - 카테고리명
  - 카테고리 아이콘
  - 예산액
  - 지출액
  - 남은 금액
  - 사용률
  - 초과 여부
- 예산이 있거나 지출이 있는 지출 카테고리만 예산 사용 목록에 포함한다.
- 초과된 카테고리를 먼저 보여주고, 그 다음 사용률과 지출액 순으로 정렬한다.

### 예산 화면 표시 개선

- 예산 탭의 카테고리 목록을 단순 지출 규모가 아니라 예산 대비 사용률 기준으로 표시하도록 변경했다.
- 각 카테고리 행에 `지출 / 예산` 금액을 표시한다.
- 남은 금액 또는 초과 금액을 함께 표시한다.
- 예산 초과 카테고리는 강조 색으로 표시한다.
- 카테고리 예산 데이터가 없으면 빈 상태 문구를 표시한다.

### README 정리

- 구현된 MVP 목록에 카테고리별 예산 사용률과 초과 표시를 반영했다.
- 다음 작업 목록에서 카테고리별 예산 사용률과 초과 표시 항목을 제거했다.

## 3. v.0.0.7 대비 상태 변경

| 항목 | v.0.0.7 상태 | v.0.0.8 상태 | 메모 |
| --- | --- | --- | --- |
| 카테고리별 예산 사용률 | 미완료 | 완료 | 월 요약 응답에 사용률 목록 추가 |
| 카테고리별 예산 초과 표시 | 미완료 | 완료 | 예산 탭에서 초과 항목 강조 |
| 예산 화면 빈 상태 | 부분 완료 | 완료 | 카테고리 예산 데이터 없음 표시 |
| Docker 빌드 검증 | 완료 | 완료 | frontend/backend 동시 빌드 통과 |

## 4. 현재 완료로 볼 수 있는 MVP 항목

- 거래 입력: 수입/지출/이체
- 거래 상세 조회
- 거래 수정
- 거래 삭제
- 일일 거래 목록
- 월 요약
- 달력 요약
- 메모 거래 목록
- 자산 목록/생성/수정/삭제
- 카드 자산 생성/수정 시 결제 계좌/확정일/결제일/자동 결제 설정
- 카테고리 목록/생성/수정/삭제
- 월별 전체 예산 및 카테고리 예산 설정
- 카테고리별 예산 사용률과 초과 표시
- 월별 수입/지출 통계
- 카테고리별 지출 비중
- 카드 결제 예약 생성/조회/실행/취소
- 카드 결제 예정 금액의 청구 기간 기반 산정
- 카드 결제 중복 실행 방지와 실패 사유 표시
- 할부 거래 생성, 일정 조회, 그룹 단위 수정/삭제
- 반복 거래 생성/수정/삭제/만기 생성
- 영수증 첨부 API 및 신규 거래 첨부 흐름
- 문자 텍스트 파싱 미리보기 초안
- Docker 기반 실행 환경

## 5. 아직 부분 완료인 항목

### 카드 관리

- 카드 결제 예약과 실행은 구현되어 있다.
- 카드 자산 생성/수정 시 카드 프로필 입력과 저장 API 연결은 구현되어 있다.
- 자동 결제 중복 실행 방지는 `PROCESSING` 상태와 낙관적 잠금으로 보강했다.
- 실패 상태 저장과 UI 표시는 구현되어 있다.
- 결제 예정 금액은 다음 결제 예정일에 대응하는 청구 기간 기준으로 산정한다.
- 말일 처리는 구현되어 있다.
- 공휴일/주말 결제일 보정 정책은 아직 미완료다.
- 실패한 카드 결제를 사용자가 다시 예약하거나 재시도하는 UI는 아직 미완료다.

### 할부 관리

- 할부 생성, 조회, 그룹 단위 수정/삭제는 구현되어 있다.
- 그룹 수정 시 기존 회차 수 변경은 아직 미지원이다.
- 카드 결제 예정 금액과 할부 청구 월 연결 검증이 필요하다.

### 예산

- 월별 전체/카테고리 예산 저장은 구현되어 있다.
- 카테고리별 예산 사용률과 초과 표시는 구현되어 있다.
- 전월 예산 복사는 미완료다.

### 통계

- 월별 및 카테고리별 지출 통계는 구현되어 있다.
- 연별 통계, 기간 필터, 카테고리 선택 후 거래 목록 이동은 미완료다.

### 영수증 첨부

- 신규 거래 첨부와 업로드 API는 구현되어 있다.
- 첨부 이미지 조회, 미리보기, 삭제, 다중 첨부 UI는 미완료다.

### 문자 자동 입력

- 원문 붙여넣기와 후보 추출은 구현되어 있다.
- 파싱 결과를 거래 입력 폼으로 확정 저장하는 흐름은 미완료다.
- 카드사/은행별 파싱 규칙과 카테고리 추천은 고도화가 필요하다.

### 공동 가계부

- Household/Member 도메인은 있다.
- 인증, 초대, 권한, 사용자 전환 UI는 미완료다.
- 인증, 권한, 사용자 전환은 불필요하더라도 자산 명의나 관련 소비 태그로 활용할 구조가 필요하다.

## 6. 다음 개발 우선순위

### v.0.0.9 권장

1. 전월 예산 복사
2. 실패한 카드 결제 재시도/재예약 UI
3. 카드 결제일 휴일 보정 정책
4. 할부 회차 수 변경 지원
5. 검색/필터

### v.0.0.10 권장

1. 영수증 이미지 조회/삭제 UI
2. 문자 파싱 결과를 거래 입력 폼으로 확정 저장
3. 연별 통계와 카테고리 선택 상세 이동
4. 데이터 내보내기
5. 자산 명의/소비 태그 구조

## 7. 검증

- `docker compose build frontend backend` 통과

## 8. 개발 메모

- 전체 월 예산 사용률은 기존 `budgetUsageRate`를 유지했다.
- 카테고리별 예산 화면은 새 `categoryBudgetUsages`를 사용한다.
- 예산이 0인 카테고리는 사용률을 0으로 두고, 지출이 있으면 목록에 표시한다.
- 로컬 PATH에서 `npm`, `git`을 찾지 못했고 백엔드에는 Gradle wrapper가 없어 로컬 직접 빌드는 실행하지 못했다.
- 프로젝트 기준 검증 방식인 Docker Compose 빌드는 정상 통과했다.
